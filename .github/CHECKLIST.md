# ✅ CI/CD Setup Checklist

Используйте этот чеклист для настройки CI/CD в первый раз.

## 📋 Первичная настройка (делается один раз)

### 1. Права GitHub Actions

- [ ] Откройте: **Settings → Actions → General**
- [ ] В разделе **Workflow permissions**:
  - [ ] Выберите: **Read and write permissions**
  - [ ] Включите: **Allow GitHub Actions to create and approve pull requests**
- [ ] Нажмите **Save**

### 2. Проверка файлов

- [x] `.github/workflows/test.yml` создан
- [x] `.github/workflows/release.yml` создан
- [x] Скрипты в `package.json` обновлены (добавлен `build:linux`)
- [x] `electron-builder.json` настроен (releaseType: "release")

### 3. Тестирование локально

Перед первым релизом проверьте что все работает локально:

```bash
# Тесты Backend
dotnet test

# Тесты Frontend
cd frontend
npm run test:run
npm run type-check
npm run lint
cd ..

# Тестовая сборка (выберите вашу платформу)
npm run build:mac-silicon  # или build:mac-intel, build:win, build:linux
```

- [ ] .NET тесты проходят
- [ ] Frontend тесты проходят
- [ ] Type check без ошибок
- [ ] Linter без ошибок
- [ ] Локальная сборка успешна

## 🚀 Первый релиз

### 4. Подготовка к релизу

- [ ] Убедитесь что все изменения закоммичены
- [ ] Обновите CHANGELOG.md (если есть)
- [ ] Проверьте текущую версию в `package.json`

### 5. Создание релиза

Выберите один из способов:

#### Вариант А: Через main (рекомендуется)
```bash
npm version 1.0.0 --no-git-tag-version
git add package.json
git commit -m "Release v1.0.0"
git push origin main
```

- [ ] Версия обновлена
- [ ] Изменения запушены в main

#### Вариант Б: Через тег
```bash
npm version 1.0.0
git tag v1.0.0
git push origin main --tags
```

- [ ] Тег создан
- [ ] Тег запушен

#### Вариант В: Ручной запуск
- [ ] Actions → Build and Release → Run workflow
- [ ] Указана версия (опционально)
- [ ] Workflow запущен

### 6. Проверка сборки

- [ ] Откройте: **Actions** на GitHub
- [ ] Найдите запущенный workflow
- [ ] Дождитесь завершения (может занять 20-30 минут)
- [ ] Проверьте что все 3 платформы собрались успешно:
  - [ ] Windows (x64)
  - [ ] macOS (Apple Silicon)
  - [ ] Linux (x64)

### 7. Проверка релиза

- [ ] Откройте: **Releases** на GitHub
- [ ] Найдите новый релиз
- [ ] Проверьте наличие всех артефактов:
  - [ ] Windows: .exe и .zip
  - [ ] macOS ARM: .dmg и .zip
  - [ ] Linux: .AppImage и .deb
- [ ] Скачайте и протестируйте хотя бы один артефакт

## 🎉 Готово!

Если все пункты отмечены, ваш CI/CD настроен и работает!

## 🐛 Что-то пошло не так?

### Ошибка прав доступа
```
Resource not accessible by integration
```
→ Проверьте **Workflow permissions** (пункт 1)

### Тесты не проходят
→ Запустите тесты локально (пункт 3)

### Сборка падает на определенной платформе
→ Проверьте логи в Actions для этой платформы
→ Убедитесь что .NET runtime и все зависимости установлены

### Релиз не появляется
→ Убедитесь что push был в ветку `main`
→ Проверьте что нет других релизов/тегов с той же версией
→ Убедитесь что все 4 платформы собрались успешно→ Проверьте что в workflow есть `permissions: contents: write`
### Ошибки "PublishManager" или "executeFinally"
→ Это устранено в обновленной версии workflow
→ Если видите эти ошибки - обновите файл release.yml
→ Артефакты все равно должны создаваться

### Linux .deb build error (author email required)
→ Убедитесь что в package.json есть author с email
→ Или добавьте maintainer в linux секцию electron-builder.json
→ Пример: `"author": { "name": "Team", "email": "support@example.com" }`

## 📚 Дополнительная информация

- [Быстрый старт (RU)](QUICK_START_RU.md)
- [Полная документация](CI_CD_SETUP.md)
- [GitHub Actions Docs](https://docs.github.com/en/actions)
- [electron-builder Docs](https://www.electron.build/)

---

**Нужна помощь?** Создайте Issue с описанием проблемы.
