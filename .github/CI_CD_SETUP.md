# CI/CD Setup for PetForRentDesktop

Этот проект настроен с автоматическим тестированием и созданием релизов через GitHub Actions.

## 🔄 Автоматические процессы

### 1. Тестирование (test.yml)

**Когда запускается:**
- При push в ветки `main`, `develop`, `work`
- При создании Pull Request в `main`

**Что делает:**
- ✅ Запускает .NET тесты (xunit)
- ✅ Проверяет типы TypeScript
- ✅ Запускает линтер
- ✅ Выполняет frontend тесты (vitest)
- ✅ Генерирует coverage отчеты

### 2. Создание релизов (release.yml)

**Когда запускается:**
- После успешного **Auto Version Bump** (workflow_run)
- Вручную через GitHub Actions UI (workflow_dispatch)

**Что делает:**
- ✅ Запускает все тесты
- ✅ Собирает приложение для всех платформ:
  - **Windows** (x64) - `.exe` установщик и `.zip`
  - **macOS** (Apple Silicon) - `.dmg` и `.zip`
  - **Linux** (x64) - `.AppImage` и `.deb`
- ✅ Сохраняет артефакты сборки
- ✅ Создает GitHub Release с артефактами
- ✅ Публикует релиз автоматически с changelog

## 🚀 Как использовать

### Вариант 1: Автоматический релиз при мердже в main

1. Убедитесь, что версия в [package.json](package.json) актуальная
2. Сделайте merge PR в ветку `main`
3. GitHub Actions автоматически:
   - Auto Version Bump повышает версию и пушит тег `vX.Y.Z`
   - Build and Release запускается после успешного Auto Version Bump
   - Проверяет наличие тега и собирает приложение для всех платформ
   - Публикует релиз с артефактами

### Вариант 2: Ручной релиз по тегу (workflow_dispatch)

1. Обновите версию в package.json:
   ```bash
   npm version 1.0.0 --no-git-tag-version
   ```

2. Закоммитьте изменения:
   ```bash
   git add package.json
   git commit -m "Bump version to 1.0.0"
   ```

3. Создайте и отправьте тег:
   ```bash
   git tag v1.0.0
   git push origin main --tags
   ```

4. Запустите **Build and Release** вручную:
   - Actions → Build and Release → Run workflow
   - Укажите версию `X.Y.Z` (или `vX.Y.Z`)

> Примечание: push тега сам по себе релиз не запускает.

### Вариант 3: Ручной запуск

1. Перейдите в: **Actions → Build and Release → Run workflow**
2. Выберите ветку `main`
3. (Опционально) Укажите версию
4. Нажмите **Run workflow**

## ⚙️ Настройка репозитория

### Обязательные настройки

1. **GitHub Token (уже настроен)**
   - GitHub автоматически предоставляет `GITHUB_TOKEN`
   - Используется для создания релизов и загрузки артефактов

2. **Проверьте права репозитория:**
   - Settings → Actions → General → Workflow permissions
   - Выберите: **Read and write permissions** ✅
   - Включите: **Allow GitHub Actions to create and approve pull requests** ✅

### Опциональные настройки (для macOS)

Для подписи и нотаризации приложений на macOS добавьте секреты:

1. **CSC_LINK** - base64 закодированный сертификат разработчика
2. **CSC_KEY_PASSWORD** - пароль от сертификата
3. **APPLE_ID** - ваш Apple ID
4. **APPLE_APP_SPECIFIC_PASSWORD** - app-specific password из Apple

Раскомментируйте эти строки в [release.yml](.github/workflows/release.yml):
```yaml
# CSC_LINK: ${{ secrets.CSC_LINK }}
# CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
# APPLE_ID: ${{ secrets.APPLE_ID }}
# APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
```

## 📦 Артефакты сборки

После успешной сборки вы получите:

### Windows
- `RentForPet-{version}-x64.exe` - установщик NSIS
- `RentForPet-{version}-x64.zip` - portable версия

### macOS (Apple Silicon)
- `RentForPet-{version}-arm64.dmg` - установщик
- `RentForPet-{version}-arm64.zip` - архив

### Linux
- `RentForPet-{version}-x86_64.AppImage` - универсальный образ
- `RentForPet-{version}-amd64.deb` - пакет для Debian/Ubuntu

## 🔍 Отладка

### Просмотр логов сборки

1. Перейдите в **Actions**
2. Выберите нужный workflow run
3. Кликните на job для просмотра логов

### Скачать артефакты напрямую

Артефакты сборки сохраняются на 7 дней для отладки:
1. Откройте workflow run
2. Прокрутите вниз до раздела **Artifacts**
3. Скачайте нужную платформу

### Частые проблемы

**Тесты не проходят:**
- Проверьте логи тестов в Actions
- Запустите тесты локально: `npm test` и `dotnet test`

**Сборка не запускается:**
- Убедитесь, что workflow permissions настроены правильно
- Проверьте, что Auto Version Bump завершился успешно

**Релиз не создается:**
- Проверьте наличие `GITHUB_TOKEN` permissions
- Убедитесь, что версия в package.json корректная
- Проверьте что нет других релизов с той же версией (тег должен быть уникальным)- **Ошибка 403**: Убедитесь что в workflow есть `permissions: contents: write`
**Сборка падает на определенной платформе:**
- Проверьте логи для конкретной платформы в Actions
- Убедитесь что .NET runtime и Node.js установлены корректно
- Проверьте что PetHotel.API/out содержит все необходимые файлы

**Ошибка "PublishManager":**
- Это нормально при использовании `--publish never`
- Артефакты все равно создаются и загружаются в релиз

**Linux .deb build error (author email):**
- Убедитесь что в package.json указан author с email
- Или добавьте maintainer в linux секцию electron-builder.json
- Пример: `"author": { "name": "Team", "email": "support@example.com" }`

**Артефакты не загружаются в релиз:**
- Проверьте что все 4 job сборки завершились успешно
- Убедитесь что тег еще не существует в репозитории

## 📝 Скрипты package.json

Новые скрипты для сборки:

```json
{
  "build:dotnet:linux": "dotnet publish PetHotel.API/PetHotel.API.csproj -c Release -r linux-x64 --self-contained -o PetHotel.API/out",
  "build:linux": "npm run build:react:electron && npm run build:dotnet:linux && electron-builder --linux --x64"
}
```

## 🔒 Безопасность

- Все секреты хранятся в GitHub Secrets
- Токены не логируются в выводе Actions
- Артефакты подписываются (если настроены сертификаты)

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [electron-builder Documentation](https://www.electron.build/)
- [Creating Releases](https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository)

## 🎯 Roadmap

- [ ] Добавить автоматическое обновление changelog
- [ ] Настроить автоматическое обновление приложений (electron-updater)
- [ ] Добавить beta/alpha каналы
- [ ] Интеграция с Sentry для отслеживания ошибок после релиза
