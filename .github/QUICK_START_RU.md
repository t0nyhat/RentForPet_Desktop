# 🚀 Быстрый старт CI/CD

## Что это дает?

✅ **Автоматическое тестирование** при каждом push
✅ **Автоматические релизы** для Windows, macOS (Intel + ARM), Linux
✅ **Номерные версии** с артефактами на GitHub

## Как использовать?

### 🎯 Способ 1: Просто мердж в main (Рекомендуется)

```bash
# 1. Обновите версию в package.json
npm version 1.2.3 --no-git-tag-version

# 2. Закоммитьте и запушьте в main
git add package.json
git commit -m "Release v1.2.3"
git push origin main

# 3. GitHub Actions автоматически создаст релиз! 🎉
```

### 🏷️ Способ 2: Создание тега

```bash
# 1. Обновите версию
npm version 1.2.3

# 2. Создайте тег и запушьте
git tag v1.2.3
git push origin main --tags

# Релиз создастся автоматически!
```

### 🖱️ Способ 3: Ручной запуск

1. Откройте GitHub → **Actions**
2. Выберите **Build and Release**
3. Нажмите **Run workflow**
4. (Опционально) укажите версию
5. Нажмите **Run workflow** ✅

## ⚙️ Первичная настройка

**ВАЖНО!** Для работы CI/CD настройте права в репозитории:

1. Откройте: **Settings → Actions → General**
2. В разделе **Workflow permissions** выберите:
   - ✅ **Read and write permissions**
   - ✅ **Allow GitHub Actions to create and approve pull requests**
3. Сохраните изменения

Готово! Теперь CI/CD работает автоматически.

## 📦 Что получите после релиза?

После успешной сборки на странице релиза будут доступны:

### Windows
- **RentForPet-{version}-x64.exe** - установщик
- **RentForPet-{version}-x64.zip** - portable версия

### macOS (Apple Silicon)
- **RentForPet-{version}-arm64.dmg** - установщик для M1/M2/M3
- **RentForPet-{version}-arm64.zip** - архив

### Linux
- **RentForPet-{version}-x86_64.AppImage** - универсальный образ
- **RentForPet-{version}-amd64.deb** - пакет для Debian/Ubuntu

## 🔍 Проверка статуса сборки

### Бейджи в README
В README есть бейджи статуса:
- 🟢 **Зеленый** = все тесты прошли / релиз успешен
- 🔴 **Красный** = есть проблемы

### Детальные логи
1. Перейдите в **Actions** на GitHub
2. Выберите нужный workflow run
3. Кликните на job для просмотра логов

## 🐛 Что делать при ошибках?

### Тесты не проходят
```bash
# Запустите тесты локально:
dotnet test                      # .NET тесты
cd frontend && npm run test:run  # Frontend тесты
```

### Сборка падает
1. Проверьте логи в Actions - кликните на failed job
2. Убедитесь что версия в package.json корректная
3. Проверьте что все зависимости установлены

### Релиз не создается
1. Проверьте **Workflow permissions** (см. выше)
2. Убедитесь что push был в ветку `main`
3. Проверьте что нет существующего релиза/тега с той же версией
4. Убедитесь что все 4 платформы собрались успешно5. **Ошибка 403**: В workflow должно быть `permissions: contents: write`
### Ошибка при публикации артефактов
- Если видите ошибки типа "PublishManager" - это нормально
- Главное что артефакты создаются и сохраняются
- Они будут загружены в релиз после успешной сборки всех платформ

## 📚 Подробная документация

Полное руководство: [CI_CD_SETUP.md](CI_CD_SETUP.md)

## 💡 Советы

- ✅ Всегда обновляйте версию перед релизом
- ✅ Следуйте [Semantic Versioning](https://semver.org/): `MAJOR.MINOR.PATCH`
- ✅ Проверяйте тесты локально перед push
- ✅ Используйте осмысленные commit сообщения
- ✅ Для beta-версий: `npm version prerelease --preid=beta`

---

**Вопросы?** Смотрите [полную документацию](CI_CD_SETUP.md) или создайте Issue.
