# Как помочь проекту

[English version →](CONTRIBUTING_EN.md)

Любая помощь приветствуется! Неважно, это опечатка в документации, баг или новая фича.

## Быстрый старт

```bash
git clone https://github.com/t0nyhat/PetForRentDesktop.git
cd PetForRentDesktop
npm install && cd frontend && npm install && cd ..
npm run dev
```

Откроется фронт на http://localhost:5173 и API на http://localhost:5226

## Что менять?

### Небольшие правки (опечатки, документация)

Отправьте PR сразу в `main`. Pre-commit checks запустятся автоматически.

### Новые фичи или баги

1. Создайте ветку: `git checkout -b feature/что-то-новое`
2. Измените код
3. Запустите тесты:
   ```bash
   npm run test          # frontend
   dotnet test          # backend
   ```
4. Отправьте PR с описанием

## Структура

```
frontend/           # React компоненты, страницы
PetHotel.API/       # .NET API с бизнес-логикой
PetHotel.Tests/     # Тесты
```

Полная справка в [DEVELOPMENT.md](DEVELOPMENT.md)

## Коммиты

Просто используйте понятные сообщения:

```
feat: добавил новую фичу
fix: исправил баг
docs: обновил документацию
```

Автоматический bump версии произойдет при мерже в main.

## Вопросы?

Откройте [Issue](https://github.com/t0nyhat/PetForRentDesktop/issues) или напишите обсуждение.

Спасибо за интерес! 🚀
